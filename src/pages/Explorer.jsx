import {useMemo,useState}from"react";import{Link}from"react-router-dom";import{opportunities}from"../data/opportunities";import OpportunityCard from"../components/OpportunityCard";import useLocalStorage from"../hooks/useLocalStorage";
const statuses=["OPEN NOW","UPCOMING","APPLICATION WINDOW NOT YET ANNOUNCED","CLOSED","YEAR-ROUND/ROLLING","NOT APPLICABLE"];
export default function Explorer({preset="all",title="Scholarship Explorer",description="Filter verified opportunities without mixing official facts and estimates.",browseMode}){const[filters,setFilters]=useState({q:"",country:"All",region:"All",funding:"All",status:"All",moi:"All"});const[favs,setFavs]=useLocalStorage("msmg-favorites",[]);const[comp,setComp]=useLocalStorage("msmg-compare",[]);const countries=[...new Set(opportunities.map(o=>o.country))].sort();const regions=[...new Set(opportunities.map(o=>o.region))].sort();const fund=[...new Set(opportunities.map(o=>o.fundingType))].sort();const update=(k,v)=>setFilters(p=>Object.assign({},p,{[k]:v}));const result=useMemo(()=>{let r=opportunities.slice();if(preset==="top10")r=r.slice().sort((a,b)=>b.score-a.score).slice(0,10);if(preset==="need")r=r.filter(o=>o.needBased==="YES");if(preset==="full")r=r.filter(o=>o.fullyFunded);if(preset==="tuition")r=r.filter(o=>o.tuitionAmount===0);if(preset==="lowcost")r=r.filter(o=>o.fundingType==="LOW-COST BACKUP");if(filters.q){const q=filters.q.toLowerCase();r=r.filter(o=>[o.university,o.program,o.scholarship,o.field,o.country].join(" ").toLowerCase().includes(q))}if(filters.country!=="All")r=r.filter(o=>o.country===filters.country);if(filters.region!=="All")r=r.filter(o=>o.region===filters.region);if(filters.funding!=="All")r=r.filter(o=>o.fundingType===filters.funding);if(filters.status!=="All")r=r.filter(o=>o.status===filters.status);if(filters.moi!=="All")r=r.filter(o=>o.moiAccepted===filters.moi);return r.sort((a,b)=>b.score-a.score)},[filters,preset]);const toggle=(setter,id)=>setter(x=>x.includes(id)?x.filter(v=>v!==id):x.length<4?x.concat(id):x);return <div className="space-y-6"><section><div className="eyebrow">{browseMode||"research explorer"}</div><h1 className="mt-2 text-3xl font-black">{title}</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500">{description}</p></section><div className="panel p-4"><div className="grid gap-3 lg:grid-cols-7"><input className="input lg:col-span-2" value={filters.q} onChange={e=>update("q",e.target.value)} placeholder="Search university, programme, scholarship"/>{[["country",countries],["region",regions],["funding",fund],["status",statuses],["moi",["YES","NO","CONDITIONAL","UNKNOWN"]]].map(([key,values])=><select className="input" key={key} value={filters[key]} onChange={e=>update(key,e.target.value)}><option value="All">{key}: All</option>{values.map(v=><option key={v}>{v}</option>)}</select>)}<button className="btn-secondary" onClick={()=>setFilters({q:"",country:"All",region:"All",funding:"All",status:"All",moi:"All"})}>Reset</button></div></div><div className="flex justify-between text-xs text-slate-500"><span>{result.length} results · bookmarks {favs.length} · compare {comp.length}</span><Link to="/comparison" className="btn-secondary">Compare selected</Link></div>{result.length?<div className="grid gap-4 xl:grid-cols-2">{result.map(o=><OpportunityCard key={o.id} o={o} favorite={favs.includes(o.id)} compared={comp.includes(o.id)} onFavorite={id=>toggle(setFavs,id)} onCompare={id=>toggle(setComp,id)}/>)}</div>:<div className="panel p-12 text-center"><h2 className="font-black">No verified records match</h2><p className="mt-2 text-sm text-slate-500">The dashboard does not add mock opportunities to fill empty filters.</p></div>}</div>}